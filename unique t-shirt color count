#include<bits/stdc++.h>
using namespace std;
int main() {
  int n,col,cnt=0;
  cin>>n;
  int count[10003];
  memset(count,0,sizeof count);
  for(int i=0;i<n;i++) {
    cin>>col;
    count[col]++;
  }
  for(int i=0;i<1003;i++) {
    if(count[i] == 1)
    cnt++;
  }
  cout<<cnt;
}
